from django.apps import AppConfig


class WasteSitesConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.waste_sites"
    label = "waste_sites"
    verbose_name = "Waste sites"
